# PlayAural - Unified Requirements
# Install with: pip install -r requirements.txt
# Recommended: use 'uv sync' inside server/ or client/ for isolated environments.

# ── Server ──────────────────────────────────────────────────────────────────
websockets>=12.0
mashumaro>=3.11
fluent-compiler>=1.0
babel>=2.14
openskill>=6.1.3
argon2-cffi>=23.1

# ── Desktop Client ───────────────────────────────────────────────────────────
wxPython>=4.2.0
accessible-output2>=0.16
sound-lib>=0.2.2
fluent-runtime>=0.4.0
requests
psutil
keyring>=24.0
livekit>=1.1.5
sounddevice>=0.5.5

# ── Development / Testing ────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pyinstaller>=6.0
